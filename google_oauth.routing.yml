google_oauth.admin_settings:
  path: '/admin/config/services/google-oauth'
  defaults:
    _form: '\Drupal\google_oauth\Form\GoogleOAuthConfigForm'
    _title: 'Google OAuth Settings'
  requirements:
    _access: 'TRUE'
google_oauth.authenticate:
  path: '/oauth/authenticate'
  defaults:
    _controller: '\Drupal\google_oauth\Controller\GoogleOAuthController::authenticate'
  requirements:
    _access: 'TRUE'
google_oauth.login:
  path: '/oauth/login'
  defaults:
    _controller: '\Drupal\google_oauth\Controller\GoogleOAuthController::login'
  requirements:
    _access: 'TRUE'

